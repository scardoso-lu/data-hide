[pytest]
testpaths = tests
pythonpath = .
addopts = -v --tb=short
markers =
    slow: tests that load the full spaCy NLP model (all anonymization tests)
    requires_spacy: skip when en_core_web_lg is not installed
