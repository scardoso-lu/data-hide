# Azure authentication
azure-identity>=1.17.0

# Delta Lake (delta-rs, no PySpark required)
deltalake>=0.18.0

# Data manipulation
pandas>=2.1.0
pyarrow>=15.0.0

# Microsoft Presidio — PII detection & anonymization
presidio-analyzer>=2.2.354
presidio-anonymizer>=2.2.354

# spaCy NLP runtime (model downloaded separately in Dockerfile)
spacy>=3.7.4,<4.0.0
